**01**
CREATE VIEW <view_name> AS <queries>;

MariaDB [partssupplydb]> CREATE VIEW view_1 AS SELECT * FROM shipments;
Query OK, 0 rows affected (0.007 sec)

**02**
DROP VIEW <view_name>;

MariaDB [partssupplydb]> DROP VIEW view_1;
Query OK, 0 rows affected (0.001 sec)

**03**
//This query is checking the database system metadata(Databases, Tables, Views, Columns, Constraints, Permissions) to find views inside a specific database.

SELECT table_schema, table_name, view_definition FROM information_schema.views WHERE table_schema = '<database_name>';